import { useParams } from 'react-router-dom'
import './EditRecord.css'

const EditRecord = () => {

  const { id } = useParams()

  return (
    <div>
      Hello
    </div>
  )
}

export default EditRecord